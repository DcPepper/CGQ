import { Box } from "@mui/material"

export default function Chart() {
    let fields = ["oui", "non", "mb", "idk"]
    return (
        <Box height={200} width={200} sx={{backgroundColor:"blue", borderRadius: "50%"}}>
        </Box>
    )
}